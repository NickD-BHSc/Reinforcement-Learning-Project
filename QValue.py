
from sre_parse import State


def episode(grid):
    state = grid.startState
    done = False
    while not done:
        nextState = robot makes choice
        calculate qValue at the next State
        check if we're done


